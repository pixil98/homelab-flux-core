apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentik-worker
  namespace: auth
  labels:
    app.kubernetes.io/name: "authentik"
    app.kubernetes.io/instance: "authentik"
    app.kubernetes.io/component: "worker"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: "authentik"
      app.kubernetes.io/instance: "authentik"
      app.kubernetes.io/component: "worker"
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "authentik"
        app.kubernetes.io/instance: "authentik"
        app.kubernetes.io/component: "worker"
    spec:
      serviceAccountName: authentik
      terminationGracePeriodSeconds: 30
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: wait-for-server
          image: docker.io/library/alpine:3.24.1
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65534
          command:
            - sh
            - -c
            - until wget -qO- http://authentik-server/-/health/ready/; do echo "waiting for authentik server"; sleep 5; done
          resources:
            requests:
              cpu: 5m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 64Mi
      containers:
        - name: worker
          image: ghcr.io/goauthentik/server:2026.5.6
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          args:
            - worker
          envFrom:
            - secretRef:
                name: authentik-env
          resources:
            requests:
              memory: "900Mi"
              cpu: "350m"
            limits:
              memory: "1536Mi"
              cpu: "700m"
          livenessProbe:
            failureThreshold: 5
            initialDelaySeconds: 5
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 5
            exec:
              command:
                - ak
                - healthcheck
          readinessProbe:
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 5
            exec:
              command:
                - ak
                - healthcheck
          startupProbe:
            failureThreshold: 60
            initialDelaySeconds: 30
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 5
            exec:
              command:
                - ak
                - healthcheck
          volumeMounts:
            - name: blueprints
              mountPath: /blueprints/custom
            - name: certificate
              mountPath: /certs/auth.${vals_info_cluster_domain}
        # Syncs Secrets labelled goauthentik.io/blueprint into /blueprints/custom
        - name: blueprint-sidecar
          image: quay.io/kiwigrid/k8s-sidecar:1.28.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 65534
          env:
            - name: LABEL
              value: goauthentik.io/blueprint
            - name: LABEL_VALUE
              value: "true"
            - name: FOLDER
              value: /blueprints/custom
            - name: RESOURCE
              value: secret
            - name: METHOD
              value: WATCH
            - name: NAMESPACE
              value: auth
            - name: UNIQUE_FILENAMES
              value: "true"
          resources:
            requests:
              cpu: 10m
              memory: 128Mi
            limits:
              cpu: 100m
              memory: 256Mi
          volumeMounts:
            - name: blueprints
              mountPath: /blueprints/custom
      volumes:
        - name: certificate
          secret:
            secretName: authentik-server-tls
            items:
              - key: tls.crt
                path: auth.${vals_info_cluster_domain}.crt
              - key: tls.key
                path: auth.${vals_info_cluster_domain}.key
        - name: blueprints
          emptyDir: {}
