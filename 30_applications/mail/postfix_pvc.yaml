apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postfix-spool
  namespace: mail
spec:
  storageClassName: truenas-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
