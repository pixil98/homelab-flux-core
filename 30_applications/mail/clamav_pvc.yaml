apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clamav-db
  namespace: mail
spec:
  storageClassName: nfs-client-nobackup
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
