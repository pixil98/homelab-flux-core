apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mapper-server-cert
  namespace: mapper
spec:
  secretName: mapper-server-tls
  dnsNames:
    - ${vals_info_cluster_domain}
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: ${vals_infra_certManager_issuer}
