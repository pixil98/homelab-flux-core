apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mud-server-characters
  namespace: games
spec:
  storageClassName: truenas-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi
